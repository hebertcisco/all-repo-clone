import cloneRepo from './cloneRepo';
import getRepos from './getRepos';
import githubURL from './githubURL';

export { cloneRepo, getRepos, githubURL };
const utils = { cloneRepo, getRepos, githubURL };
export default utils;
